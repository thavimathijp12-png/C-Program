#include <stdio.h>

int main() {
    int n;
    int i;
    int expsum, acsum = 0;
    int roll;
    scanf("%d", &n);
    for (i = 0; i < n - 1; i++) {
        scanf("%d", &roll);
        acsum += roll;
    }
    expsum = n*(n + 1)/2;
    printf("%d", expsum - acsum);

    return 0;
}
