#include <stdio.h>
int main() {
    int N,sum=0;
    scanf("%d", &N);
    for (int i=1; i < N; i++) {
        if (N%i == 0) {
            sum = sum + i;
        }
    }
    if (sum == N && N > 0) {
        printf("Yes\n");
    } else {
        printf("No\n");
    }
    return 0;
}
