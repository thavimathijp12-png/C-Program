#include<stdio.h>
int main() {
    int N;
    int add;
    scanf("%d",&N);
    for (int i=1; i<=N; i++) {
        if (i % 2 == 0) {
            add=add+i;
        }
        
    }
    printf("%d",add);
    return 0;
}