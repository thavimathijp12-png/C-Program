#include <stdio.h>
int main() {
    int A, B, max;
    scanf("%d %d", &A, &B);
    max = (A > B) ? A : B;
    for (int i = max; ; i++) {
        if (i % A == 0 && i % B == 0) {
            printf("%d\n", i);
            break;
        }
    }
    return 0;
}
